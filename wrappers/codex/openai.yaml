interface:
  display_name: "Local RAG KB"
  short_description: "Build and query a local file-backed RAG knowledge base."
  default_prompt: "Use $local-rag-kb to ingest a local file or archive into a KB, emit a host answer bundle, and answer with citations."

policy:
  allow_implicit_invocation: true
